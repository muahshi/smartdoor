# SmartDoor Android — ProGuard/R8 rules.
# Phase A1.1: no rules needed yet (no networking/serialization/WebRTC libs present).
# Future phases will add -keep rules here as those libraries are introduced.
