<?xml version="1.0" encoding="utf-8"?>
<!-- Phase 12E.8 — PREMIUM SMART PLATE ECOSYSTEM. Exposes only the
     app-cache subfolder QrImageUtil.share writes the shared QR PNG into —
     nothing else in app storage is reachable via the FileProvider. -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="shared_qr" path="shared_qr/" />
</paths>
